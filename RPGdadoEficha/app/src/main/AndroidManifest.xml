<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permissão para usar a vibração -->
    <uses-permission android:name="android.permission.VIBRATE"/>

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.RPGdadoEficha"
        tools:targetApi="31">

        <!-- Atividades do aplicativo -->
        <activity
            android:name=".Tela100lados"
            android:exported="false" />
        <activity
            android:name=".Tela20lados"
            android:exported="false" />
        <activity
            android:name=".Tela12lados"
            android:exported="false" />
        <activity
            android:name=".Tela10lados"
            android:exported="false" />
        <activity
            android:name=".Tela8lados"
            android:exported="false" />
        <activity
            android:name=".Tela4lados"
            android:exported="false" />
        <activity
            android:name=".Tela6lados"
            android:exported="false" />
        <activity
            android:name=".MainActivity"
            android:exported="true">

            <!-- Intent-filter para a atividade principal -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
